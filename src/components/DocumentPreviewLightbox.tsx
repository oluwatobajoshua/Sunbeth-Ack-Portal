// Placeholder module to satisfy TypeScript's isolatedModules requirement.
// The original implementation was removed during refactors. Keep this file
// as a module so build tooling and imports resolve correctly.

export {}
